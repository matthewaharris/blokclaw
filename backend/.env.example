# Server
PORT=3000
NODE_ENV=development

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blokclaw_dev
DB_USER=matthewharris
DB_PASSWORD=

# Auth (generate a secure secret for production)
JWT_SECRET=your-secret-key-here

# Email (set EMAIL_DEV_MODE=true to log verification links to console)
EMAIL_DEV_MODE=true
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@blokclaw.com

# Admin
ADMIN_SECRET=your-admin-secret-here

# Health Checks (cron expression)
HEALTH_CHECK_INTERVAL=0 * * * *
HEALTH_CHECK_THRESHOLD=3

# App
APP_URL=http://localhost:5173
